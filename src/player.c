#include <ncurses.h>

#include "main.h"
#include "mapgen.h"
#include "player.h"

/*struct Player initial_player(){
	struct Player player = {0, 0, 0, 0, 0, 0};
	return player;
}*/
//Spawns the player in a chunk
/*struct Player spawnPlayer(struct Chunk c){
	struct Player p;

}
//Circles around the outer edges of the tilemap to find a suitable space.
struct Player findGoodSpace(struct Player p, struct Chunk c, int y, int x){

}*/


